# npm 11+ requires an interactive bulk-delete confirmation for `npm ci`
# cleanups (SAFE_DELETE_BULK_CONFIRM_REQUIRED) that fails in CI / non-TTY
# builds. Disable it so the frontend build is fully non-interactive.
safe-delete=never
